// Package mocks — моки mockery для тестового wire-контейнера (internal/di/ditest).
//
// Сами моки (Repository.go и далее) создаёт `make generate`; их принято
// коммитить — CI проверяет, что сгенерированный код актуален.
//
// Этот файл мока не содержит и нужен только для того, чтобы пакет
// существовал до первого запуска mockery: в `make generate` шаг
// `go mod tidy` идёт раньше, и без каталога он пытается разрешить
// [[.Module]]/internal/mocks как внешний модуль — то есть уходит в сеть
// за несуществующим репозиторием и падает. Удалять не нужно.
package mocks
