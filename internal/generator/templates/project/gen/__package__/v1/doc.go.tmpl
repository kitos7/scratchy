// Package [[.Package]]v1 — код, сгенерированный buf по контракту
// [[.Package]].v1: pb-типы, gRPC-клиент и сервер, HTTP-gateway.
// Файлы создаёт `make generate`, править их руками не нужно.
//
// Сам этот файл ничего не объявляет и нужен только для того, чтобы пакет
// существовал до первого запуска buf: иначе `go mod tidy` — в том числе
// запущенный иде — пытается разрешить [[.Module]]/gen/[[.Package]]/v1
// как внешний модуль и падает с «cannot find module providing package».
// Удалять не нужно.
package [[.Package]]v1
